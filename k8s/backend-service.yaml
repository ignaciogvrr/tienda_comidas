apiVersion: v1
kind: Service
metadata:
  name: tienda-comidas-backend
  namespace: tienda-comidas
spec:
  selector:
    app: tienda-comidas-backend
  ports:
    - port: 3001
      targetPort: 3001
  type: ClusterIP
