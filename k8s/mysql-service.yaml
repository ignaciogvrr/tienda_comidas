apiVersion: v1
kind: Service
metadata:
  name: tienda-comidas-db
  namespace: tienda-comidas
spec:
  selector:
    app: tienda-comidas-db
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  clusterIP: None
